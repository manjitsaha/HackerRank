 public static List<Integer> rotateLeft(int d, List<Integer> arr) {
    
        for(int i = 0 ; i < d ; i++) {
            int value = arr.get(0);
            arr.remove(0);
            arr.add(value);
        }
        
        return arr;
    }
